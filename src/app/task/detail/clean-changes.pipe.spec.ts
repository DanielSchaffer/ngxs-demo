import { CleanChangesPipe } from './clean-changes.pipe';

describe('CleanChangesPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanChangesPipe();
    expect(pipe).toBeTruthy();
  });
});
